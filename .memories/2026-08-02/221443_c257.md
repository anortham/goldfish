---
id: checkpoint_c257b8a9
timestamp: 2026-08-02T22:14:43.921Z
tags:
  - release
  - 7.6.2
  - verification
  - ci
  - github-actions
git:
  branch: main
  commit: a39f621
  files:
    - CLAUDE.md
summary: Goldfish 7.6.2 released and verified
type: checkpoint
evidence:
  - "gh run 30769571949 (main push): success"
  - "gh run 30769642674 (tag push): test success, version-tag success"
  - https://github.com/anortham/goldfish/releases/tag/v7.6.2
next: "None — release complete. Optional later: TS7 tsconfig migration, Windows
  CI runner."
---

## Goldfish 7.6.2 released and verified

- **Pushed:** main (`a39f621`) + annotated tag `v7.6.2`; `check:version-tag` green on the tagged commit locally and in CI
- **GitHub releases:** v7.6.2 published and marked latest; v7.6.1 backfilled (its tag had been pushed on 2026-07-17 without a release)
- **CI verified live, first ever runs:** main-push run green in 17s; tag-path run green with both jobs (`test`, `version-tag`)

### Release-process discovery
Pushing branch + tag in ONE `git push` coalesced the events — the tag never triggered a workflow run. Verified the tag path by pushing a throwaway tag `v7.6.2-ci-wiring-test` at the release commit (the guard checks all tags on HEAD, so it validated the real v7.6.2 in CI), then deleted it. **Documented in CLAUDE.md: push the branch and the tag in separate commands.**

Also: pushes on this machine need gh's credential helper (`git -c credential.helper='!gh auth git-credential' push ...`) — plain `git push` hangs on an interactive credential prompt.
