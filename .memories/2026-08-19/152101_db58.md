---
id: checkpoint_db58b5b9
timestamp: 2026-08-19T15:21:01.732Z
tags:
  - design
  - audit-trail
  - g1
  - g2
  - actor
  - worktree
git:
  branch: main
  commit: 2e2392d
  files:
    - docs/plans/2026-08-19-checkpoint-actor-and-worktree-git-design.md
summary: Designed G1 actor identity and G2 worktree-accurate git
briefId: post-7-8-0-evidence-driven-hardening-across-client
type: decision
decision: G1+G2 land as one PR with two commits (G2 then G1). Actor is
  server-observed only. Git queries run at the same-repo worktree toplevel, not
  at process.cwd().
alternatives:
  - Actor as tool args (untrusted)
  - Always query git at process.cwd() (breaks plugin cwd and subdirectory file
    paths)
  - New src/actor.ts module (too much for a YAML block)
  - Two PRs (conflict-prone on format/parse)
impact: Unblocks a small implementation plan after the user reviews the spec. No
  code yet.
evidence:
  - docs/plans/2026-08-18-audit-trail-gap-assessment.md
  - docs/plans/2026-08-19-checkpoint-actor-and-worktree-git-design.md
  - Reviewer approve after 3 rounds
symbols:
  - saveCheckpoint
  - getGitContext
  - handleCheckpoint
  - presentCheckpoint
  - resolveGitCaptureCwd
  - assembleActor
next: User reviews the spec. If approved, write the implementation plan in a
  worktree (do not implement on main).
confidence: 5
unknowns:
  - Exact MCP v2 envelope slot for client name — pin with a modern spawned test
    during implementation
---

## Designed G1 actor identity and G2 worktree-accurate git

- **WHAT:** Wrote `docs/plans/2026-08-19-checkpoint-actor-and-worktree-git-design.md` from the 2026-08-18 audit-trail assessment. Writer/reviewer loop ran 3 review rounds and approved with 0 open issues. Not committed (still on `main`).
- **WHY:** G1 has no harness/model/session/user on checkpoints. G2 was observed live: a worktree save stamped `main` because `saveCheckpoint` runs git on the registered root.
- **HOW:** Actor is server-observed only (no tool args). Env beats MCP. `src/server.ts` is the only module that reads MCP ctx. Git probes `--git-common-dir` at caller cwd, then queries at `--show-toplevel` so `git.files` stay repo-root relative. `git.worktree` is an absolute host path when toplevel differs. Memories still write under workspace `.memories/`.
- **IMPACT:** Spec is ready for user review, then an implementation plan. G3–G6 stay out of scope.

