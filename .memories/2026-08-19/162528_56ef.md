---
id: checkpoint_56efe404
timestamp: 2026-08-19T16:25:28.233Z
tags:
  - sdd
  - g1-actor
  - g2-worktree-git
  - plan-complete
git:
  branch: checkpoint-actor-worktree-git
  commit: eb1e3b0
  files:
    - docs/plans/2026-08-19-checkpoint-actor-and-worktree-git.md
summary: "Plan complete: checkpoint actor identity + worktree-accurate git"
briefId: post-7-8-0-evidence-driven-hardening-across-client
type: checkpoint
evidence:
  - "bun test: 676 pass / 0 fail (21 files) at eb1e3b0"
  - tsc --noEmit clean
  - "modern envelope pin: wrong-key mutation failed only the modern spawned test"
symbols:
  - resolveGitCaptureCwd
  - assembleActor
  - formatActorLine
  - extractObservedActor
  - getGitIdentity
  - saveCheckpoint
next: "Awaiting user decision: merge to main / push / release are unapproved"
---

## Plan complete: checkpoint actor identity + worktree-accurate git

**WHAT:** Both tasks of `docs/plans/2026-08-19-checkpoint-actor-and-worktree-git.md` landed on branch `checkpoint-actor-worktree-git`. Task 1 (G2 worktree git) at `de6137d`; Task 2 (G1 actor block) at `eb1e3b0`. Branch gate: `bun test` 676 pass / 0 fail, `tsc --noEmit` clean.

**WHY:** G1/G2 from the 2026-08-18 audit-trail gap assessment: checkpoints lacked who-produced-them, and worktree saves stamped the main checkout's branch.

**HOW (key outcomes):**
- `resolveGitCaptureCwd` probes `--git-common-dir` at caller cwd, queries git at `--show-toplevel`, records absolute `git.worktree` when it differs from the workspace. Compact+file recall copies git without mutating the day cache.
- Server-observed `actor` block (`harness`, `model`, `session`, `user`, `git_user`, `git_email`); env beats MCP; never from tool args (stuffed keys proven ignored). `extractObservedActor` in `src/server.ts` only.
- Modern-era harness pinned to envelope slot `io.modelcontextprotocol/clientInfo` (proved by a wrong-key mutation test run); legacy era via `getClientVersion()`. Both eras tested through real protocol clients.
- Test isolation: preload deletes `GOLDFISH_HARNESS/MODEL/SESSION`; default `getOsUsername`/`getGitIdentity` stubs keep host identity out of fixtures.

**IMPACT:** Checkpoints are now audit-complete (who + which worktree). No version bump — changelog `[Unreleased]` carries Fixed + Added. Branch is local only: no push, no tag, no merge without approval. SDD ledger and reports were in `.razorback/sdd/` (deleted after gate; git history is the record).
