---
id: checkpoint_863743b3
timestamp: 2026-08-19T18:07:34.130Z
tags:
  - release
  - v7.9.0
  - g1-actor
  - g2-worktree-git
git:
  branch: main
  commit: fdbbd85
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .cursor-plugin/plugin.json
    - CHANGELOG.md
    - README.md
    - package.json
    - src/server.ts
summary: "Release 7.9.0: audit-complete checkpoints"
briefId: post-7-8-0-evidence-driven-hardening-across-client
type: checkpoint
evidence:
  - "bun test: 680 pass / 0 fail after version bump"
  - tsc --noEmit clean
  - "codex pre-merge review: 2 findings fixed, 2 adjudicated by-design"
---

## Release 7.9.0: audit-complete checkpoints

**WHAT:** Merged `checkpoint-actor-worktree-git` into main (fast-forward to `fdbbd85`) and bumped 7.8.0 → 7.9.0 across all seven version surfaces plus a `## [7.9.0]` changelog section and a new README banner.

**WHY:** Ships G1 + G2 from the audit-trail gap assessment: worktree-accurate git capture (`resolveGitCaptureCwd`, `git.worktree`) and the server-observed `actor` frontmatter block. Closes the live bug where worktree saves stamped the main checkout's branch.

**HOW:** Plan executed via subagent-driven development (two serial tasks, lead inline review, TDD throughout). External Codex review before merge found two real issues, both fixed in `fdbbd85`: per-source identity guards and actor-value sanitization (control-character collapse + 200-char cap). Two contested findings recorded as by-design: session recording (stdio yields no session id — proven by probe) and caller-tree precedence (Key Decision 8).

**IMPACT:** Checkpoints now record who (harness/model/user/git identity, unspoofable via tool args) and which tree (git.worktree). Verification: `bun test` 680 pass / 0 fail with version-sync tests, `tsc --noEmit` clean.

**NEXT:** Tag v7.9.0, run `bun run check:version-tag`, push main and the tag in separate pushes (7.6.2 lesson), then remove the merged task worktree and branch.
