---
id: checkpoint_b0d4c5b7
timestamp: 2026-04-22T13:14:12.700Z
tags:
  - patch-release
  - bug-fix
  - checkpoint
  - recall
  - briefs
  - workspace
  - version-bump
  - release
git:
  branch: main
  commit: e06ff1f
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex/config.toml
    - CHANGELOG.md
    - README.md
    - docs/plans/2026-04-22-reviewed-defects-fix.md
    - package.json
    - src/checkpoints.ts
    - src/handlers/brief.ts
    - src/handlers/checkpoint.ts
    - src/recall.ts
    - src/server.ts
    - src/workspace.ts
    - tests/checkpoints.test.ts
    - tests/handlers.test.ts
    - tests/recall.test.ts
    - tests/workspace.test.ts
summary: Fixed reviewed defects and prepared v7.0.2 release
briefId: v7-0-0-subtract-sprint-then-handoff
type: checkpoint
context: User asked to fix the reviewed issues, then bump version numbers and
  push the resulting patch release.
decision: Ship the reviewed fixes as a patch bump from 7.0.1 to 7.0.2 and keep
  the implementation surgical rather than expanding into broader cleanup.
alternatives:
  - Leave the version at 7.0.1 and push code-only fixes, which would make the
    release surfaces lie about the shipped patch.
  - Broaden the work into more parser hardening and legacy cleanup, which would
    delay the requested push and widen scope beyond the reviewed defects.
impact: Patch-release surfaces are aligned, regression coverage increased, and
  the commit will carry a checkpoint that explains the fix set.
evidence:
  - bun test tests/server.test.ts
  - bun test
  - bun run typecheck
symbols:
  - saveCheckpoint
  - parseJsonCheckpoint
  - recall
  - handleBrief
  - handleCheckpoint
  - ensureMemoriesDir
  - assertProjectWorkspace
next: Commit the patch release, excluding `.codex/config.toml`, then push the
  branch to origin.
confidence: 5
unknowns:
  - Whether the user wants `.codex/config.toml` committed despite its
    machine-local absolute paths.
---

## Fixed reviewed defects and prepared v7.0.2 release

Closed the concrete bugs from the review pass and bumped the release surfaces for a patch release.

- **What:** Fixed checkpoint ID collisions, awaited cross-workspace registration, hardened legacy JSON checkpoint parsing, corrected cross-workspace search summaries, coerced brief tag arrays from MCP string inputs, rejected `workspace: "all"` on write paths, and stopped eagerly creating the dead `.memories/plans/` directory.
- **Why:** The old behavior could break search recall, hide freshly saved work from cross-project recall, surface corrupted legacy data, and create stray `.memories/` data under a literal `./all/` path.
- **How:** Added regression tests first, applied minimal fixes in `src/checkpoints.ts`, `src/recall.ts`, `src/handlers/brief.ts`, `src/handlers/checkpoint.ts`, and `src/workspace.ts`, then bumped `package.json`, `src/server.ts`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, and `CHANGELOG.md` from `7.0.1` to `7.0.2`.
- **Impact:** The patch release now reflects shipped behavior, the reviewed defects are covered by tests, and immediate cross-workspace recall sees newly saved work reliably.
