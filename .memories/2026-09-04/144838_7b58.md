---
id: checkpoint_7b586839
timestamp: 2026-09-04T14:48:38.723Z
tags:
  - release
  - 8.0.2
  - hooks
  - codex
  - cursor
git:
  branch: fix/codex-session-hook-json
  commit: aae371d
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .cursor-plugin/plugin.json
    - AGENTS.md
    - CHANGELOG.md
    - CLAUDE.md
    - README.md
    - docs/IMPLEMENTATION.md
    - docs/agent-portability.md
    - docs/plans/2026-07-16-hooks-tier-design.md
    - hooks/session-start.ts
    - package.json
    - src/server.ts
    - tests/hooks.test.ts
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Prepared Goldfish 8.0.2 for release
type: checkpoint
impact: Restores Codex SessionStart guidance delivery, keeps Claude Code
  compatibility, ships the Cursor plugin-root correction, and synchronizes all
  release metadata at 8.0.2.
evidence:
  - "bun test: 672 pass, 1 skip, 0 fail"
  - "bun run typecheck: tsc --noEmit passed"
  - git diff --check passed
  - Direct SessionStart envelope replay passed
  - "bun test tests/hooks.test.ts: 22 pass, 0 fail"
  - Official Codex SessionStart contract verified at
    https://developers.openai.com/codex/hooks#sessionstart
symbols:
  - SERVER_VERSION
  - getHookContext
next: Commit the verified release tree, merge to main, tag v8.0.2, run the
  tag-version check, push main and tag separately, and publish the GitHub
  release.
confidence: 5
---

## Prepared Goldfish 8.0.2 for release

### WHAT

- Corrected `hooks/session-start.ts` to emit the nested Codex SessionStart `hookSpecificOutput` JSON envelope.
- Added regression coverage for the exact JSON shape while preserving exit-0 failure containment.
- Updated maintained hook documentation and corrected the dated design's stale raw-stdout claims.
- Bumped all seven version surfaces to `8.0.2` and added the 2026-09-04 changelog entry.

### WHY

Current Codex hook documentation requires SessionStart JSON context under `hookSpecificOutput.additionalContext`; raw stdout no longer delivered the installed Goldfish guidance reliably. The patch also packages the Cursor plugin-root fix and documentation additions already on `main` since `v8.0.1`.

### HOW

- Verified the contract against https://developers.openai.com/codex/hooks#sessionstart and confirmed the same envelope remains valid for the shared Claude Code hook.
- Preserved Cursor's separate `{ additional_context }` hook contract.
- Used the existing version-sync tests as the release metadata contract: changing only `SERVER_VERSION` produced the expected mismatch failure, then updating the remaining surfaces restored green.
- Reviewed the combined diff with Miller impact analysis and two independent Luna audits.

### IMPACT

Goldfish 8.0.2 is ready for a release commit, fast-forward merge to `main`, separate branch and tag pushes, and a GitHub release. The only remaining user action is re-trusting the edited Herdr hook through `/hooks` by pressing `t`.
