---
id: checkpoint_ddb0bc71
timestamp: 2026-09-04T22:37:31.533Z
tags:
  - review
  - bug-fix
  - search
  - duplicate-ids
  - cache
git:
  branch: main
  commit: a6bae48
  files:
    - README.md
    - docs/IMPLEMENTATION.md
    - src/ranking.ts
    - tests/ranking.test.ts
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
evidence:
  - "bun test: 676 pass, 1 skip, 0 fail"
  - "bunx tsc --noEmit: exit 0"
  - "git diff --check: exit 0"
symbols:
  - searchCheckpoints
  - buildIndex
  - getIndex
  - checkpointSearchIdentity
---

## WHAT
Fixed review findings in src/ranking.ts and tests/ranking.test.ts: duplicate persisted checkpoint IDs no longer crash Orama search; cached subset searches no longer lose valid records to excluded hits or suppress partial matches because excluded exact matches exist. Corrected README.md and docs/IMPLEMENTATION.md cache wording.

## WHY
Markdown can be edited or imported, and one duplicate ID should not disable search across the corpus. Cache reuse must preserve selected-candidate retrieval behavior.

## HOW
Added deterministic private search IDs with occurrence suffixes, preserving public checkpoint IDs. Cached indexes retain document counts; candidate filtering precedes exact-to-partial fallback. Four regressions cover distinct duplicate contents, copied subsets, partial fallback, and identical repeated records. Observed red failures before fixes. Final bun test: 676 pass, 1 Windows-only skip, 0 fail; bunx tsc --noEmit and git diff --check passed.

## IMPACT
Search handles duplicate IDs and cached subsets without changing persisted files, schemas, or dependencies. Product review still recommends outcome measurement before expanding features. Full native Windows runtime verification remains outside this Linux review.
