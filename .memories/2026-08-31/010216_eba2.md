---
id: checkpoint_eba28e3e
timestamp: 2026-08-31T01:02:16.773Z
tags:
  - workspace-binding
  - typecheck
  - verification
git:
  branch: codex/fail-closed-workspace-identity
  commit: "2041675"
  files:
    - src/server.ts
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: post-7-8-0-evidence-driven-hardening-across-client
type: checkpoint
symbols:
  - hydrateWorkspaceArguments
  - ResolveWorkspaceOptions
next: Commit the exact-optional roots-shape fix, rerun typecheck, affected
  integration, and the full suite, then reconcile worktrees.
---

## WHAT
Fixed the final TypeScript integration error in `hydrateWorkspaceArguments`: when legacy Roots is unavailable, the resolver options object now omits the optional `roots` property instead of explicitly passing `undefined`.

## WHY
`ResolveWorkspaceOptions.roots` is optional but does not include explicit `undefined` under `exactOptionalPropertyTypes`. Runtime tests passed, but the branch typecheck correctly rejected the object shape with TS2379.

## HOW
Systematic debugging traced the bad value from the local `Root[] | null | undefined` variable into `resolveWorkspaceWithSource`. A bounded Luna correction used a conditional object spread, preserving arrays and `null` while omitting only `undefined`. `bun run typecheck` changed from TS2379/exit 2 to exit 0; `bun test server workspace-recovery protocol-compatibility` passed 68 tests; lead reviewed the one-line diff.

## IMPACT
The async workspace adapter now satisfies exact optional-property typing without weakening the public interface or changing runtime binding behavior. Final affected and full-suite gates must be rerun because HEAD will change.
