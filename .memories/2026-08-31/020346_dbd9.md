---
id: checkpoint_dbd94b62
timestamp: 2026-08-31T02:03:46.184Z
tags:
  - release
  - 8.0.0
  - workspace-binding
git:
  branch: codex/fail-closed-workspace-identity
  commit: 9af7a08
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .cursor-plugin/plugin.json
    - CHANGELOG.md
    - README.md
    - package.json
    - src/server.ts
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Prepared Goldfish 8.0.0 release
type: checkpoint
evidence:
  - "bun test server agent-assets: 71 pass, 0 fail"
  - "bun run typecheck: passed"
  - "bun test: 672 pass, 1 Windows-only skip, 0 fail"
  - "git diff --check: passed"
symbols:
  - SERVER_VERSION
  - resolveVerifiedWorkspace
  - hydrateWorkspaceArguments
next: Commit the release preparation, recheck every related worktree, create
  v8.0.0, run the tag/version guard, then push the branch and tag in separate
  commands.
---

## Prepared Goldfish 8.0.0 release

### WHAT

Bumped all seven release version surfaces from `7.9.0` to `8.0.0`, converted the changelog's Unreleased entry into the dated `8.0.0` release section, and updated the README banner to describe fail-closed explicit workspace identity.

### WHY

The workspace-binding change intentionally breaks ambiguous user-level MCP behavior: mutating tools and current-project recall now require a verified absolute project root unless a project-level environment binding or supported Roots signal provides it. A major version makes that contract explicit.

### HOW

Kept the release isolated in the clean `codex/fail-closed-workspace-identity` worktree. Left the main checkout's machine-local MCP command-path edits untouched. Verified the version-sync test failed after changing only `SERVER_VERSION`, then updated the other six surfaces and release notes.

### IMPACT

`v8.0.0` is ready to commit, tag, and push from the feature branch. User-level Codex, Cursor, and other MCP registrations must send the conversation workspace on every workspace-aware call; ambiguous cwd, registry, and parent-walk candidates no longer authorize access.
