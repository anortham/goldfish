---
id: checkpoint_69a439a2
timestamp: 2026-07-16T14:26:04.174Z
tags:
  - hooks
  - implementation
  - review
  - codex
  - claude-code
git:
  branch: main
  commit: 299a3c4
  files:
    - .claude/worktrees/hooks-tier/
summary: Hooks tier implemented and review-approved
type: checkpoint
evidence:
  - "bun test: 614 pass / 0 fail at c468a6d"
  - tsc --noEmit clean
  - "bun hooks/session-start.ts: exit 0, 3479 chars"
  - "RED observed: Cannot find module '../src/hook-context' before
    implementation"
symbols:
  - getHookContext
  - TOOL_QUICK_REFERENCE
  - buildUsageDoc
next: "Finish the branch (razorback:finishing-a-development-branch): merge/PR
  decision, then manual smokes (Claude Code plugin install, Codex install +
  /hooks trust) before any release"
---

## Hooks tier implemented and review-approved

**WHAT:** Full hooks-tier implementation on branch `worktree-hooks-tier` (worktree `.claude/worktrees/hooks-tier`), commits `2121441` (feat: hook content + script + manifests + 13 tests), `c468a6d` (docs: portability reversal, README Codex plugin route, six version surfaces), `ad1fa71` (acceptance criteria ticked after lead review).

**WHY:** Restores the ambient behavioral surface that the 2k instruction cap + deferred tool loading removed.

**HOW:**
- `hooks/goldfish-hooks.json` — one SessionStart event, matcher `startup|clear|compact`, bun command + PowerShell variant, shared by `.claude-plugin/plugin.json` and new `.codex-plugin/plugin.json`
- `hooks/session-start.ts` — branchless, EPIPE-safe, raw stdout, always exit 0
- `src/hook-context.ts` — `getHookContext()` = `getInstructions()` verbatim + tool advertisement + shared `TOOL_QUICK_REFERENCE` (now imported by `build-usage-doc.ts`) + WHAT/WHY/HOW/IMPACT block; 3,479 chars vs 10k cap
- `.codex-plugin/mcp.json` bundles the MCP server — Codex install is one command pair (`codex plugin marketplace add anortham/goldfish`, `codex plugin add goldfish@goldfish`) plus one-time `/hooks` trust review
- Version surfaces now six (`.codex-plugin/plugin.json` added), test-enforced

**IMPACT:** Implementer (subagent) followed TDD with observed RED; lead review found zero issues to send back — 6 judgment calls all sound (best: CONTRIBUTING got a pointer to CLAUDE.md's list instead of a duplicate). Branch gate: 614 pass / 0 fail, tsc clean.
