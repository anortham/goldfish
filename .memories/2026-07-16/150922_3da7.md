---
id: checkpoint_3da7b10a
timestamp: 2026-07-16T15:09:22.264Z
tags:
  - hooks
  - codex
  - review
  - release-prep
git:
  branch: codex/fix-goldfish-hook-review
  commit: 809d11d
  files:
    - .agents/skills/recall/SKILL.md
    - .claude/worktrees/hooks-tier/.julieignore
    - .codex-plugin/mcp.json
    - .codex-plugin/plugin.json
    - .mcp.json
    - AGENTS.md
    - CLAUDE.md
    - README.md
    - docs/IMPLEMENTATION.md
    - docs/agent-instructions/goldfish-usage.md
    - docs/agent-portability.md
    - docs/plans/2026-07-16-hooks-tier-design.md
    - hooks/session-start.ts
    - skills/recall/SKILL.md
    - src/hook-context.ts
    - src/instructions.ts
    - src/tools.ts
    - tests/hooks.test.ts
    - tests/server.test.ts
summary: Codex validation review accepted with fixes — hooks tier corrected for
  real Codex installs
type: decision
decision: Accept Codex's install-validated corrections (root .mcp.json, hardened
  hook script, conditional-recall language) minus the design-doc-pinning test;
  align the two surfaces it missed
alternatives:
  - "Keep .codex-plugin/mcp.json and reject the root move (rejected: Codex
    validated the canonical location against a real install, and the wrapped
    format is valid Claude Code project config too)"
  - "Keep the design-doc-pinning test (rejected: couples the suite to historical
    plan prose)"
evidence:
  - "bun test: 617 pass / 0 fail (stable across 3 runs)"
  - tsc --noEmit clean
  - instructions 1923 chars, hook payload 3712 chars
symbols:
  - getHookContext
  - getInstructions
  - TOOL_QUICK_REFERENCE
next: Bump six surfaces to 7.6.0 + CHANGELOG, then tag/push/release pending user
  approval
---

## Codex validation review accepted with fixes — hooks tier corrected for real Codex installs

**WHAT:** Reviewed and finalized Codex's uncommitted validation changes on branch `codex/fix-goldfish-hook-review` (user ran Codex to validate the Codex-specific hooks integration).

**WHY:** Codex validated against a real install and corrected three design assumptions: (1) the canonical Codex MCP map is a root `.mcp.json` with a `mcpServers` wrapper — not `.codex-plugin/mcp.json`; (2) relative `./src/server.ts` + `cwd: "."` beats `${PLUGIN_ROOT}` and doubles as valid Claude Code project config when the goldfish repo itself is opened; (3) 10k is a Goldfish safety budget, not a documented harness truncation limit.

**HOW (accepted):** root `.mcp.json` move; hook script hardening (guarded dynamic import, contained stderr reporting, failure-containment subprocess test); conditional-recall philosophy (recall on resume/context-loss/user-ask/relevance, not reflexive session-start) and recalled-context-as-historical-evidence (verify drift-prone facts) across instructions, tool description, and test pins.

**HOW (my fixes on top):** removed Codex's test block that pinned prose inside the dated design doc (plans are point-in-time records; the 10k invariant is already enforced on getHookContext()); aligned two surfaces Codex missed — skills/recall/SKILL.md and CLAUDE.md's Behavioral Language Pattern — with the conditional-recall philosophy; re-synced mirrors.

**IMPACT:** 617 tests pass, typecheck clean. Instructions now 1,923/2,000 chars — only 77 headroom left; next instruction edit must watch the cap. Ready for 7.6.0 release prep.
