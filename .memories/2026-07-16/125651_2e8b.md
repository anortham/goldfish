---
id: checkpoint_2e8b70d7
timestamp: 2026-07-16T12:56:51.668Z
tags:
  - review-fixes
  - track-4
  - harness-support
  - portability
  - agents-md
  - version-check
  - drift-guards
  - opencode
  - vscode
git:
  branch: main
  commit: 0c23e29
  files:
    - .gitignore
    - .vscode/mcp.json
    - AGENTS.md
    - CLAUDE.md
    - README.md
    - docs/agent-instructions/goldfish-usage.md
    - docs/agent-portability.md
    - docs/plans/2026-07-16-review-fixes-plan.md
    - opencode.json
    - package.json
    - scripts/build-usage-doc.ts
    - scripts/sync-agent-skills.ts
    - scripts/version-tag-check.ts
    - tests/agent-assets.test.ts
summary: "Track 4: harness tier-1 — instruction-tier ruleset, drift guards,
  committed configs"
briefId: review-fixes-correctness-brief-adoption-perf-harne
type: checkpoint
impact: Instruction tier unlocked for a dozen harnesses via one generated file;
  version/mirror drift now fails tests instead of shipping; portability
  decisions recorded so they stop being re-litigated.
evidence:
  - "bun test: 596 pass / 0 fail (17 files, 7 new agent-asset tests)"
  - tsc --noEmit clean
  - bun run check:version-tag exercises the standalone guard
symbols:
  - buildUsageDoc
  - checkVersionAgainstTag
  - getExactTagForHead
next: All four review-fix tracks complete. Remaining follow-ups are
  release-shaped (version bump to 7.5.0, CHANGELOG, tag) and need user approval.
---

## Track 4: harness tier-1 — instruction-tier ruleset, drift guards, committed configs

Final review-fix track (plan: docs/plans/2026-07-16-review-fixes-plan.md). Strategy per the ponytail comparison: port its discipline (generators, drift guards, documented negative space), not its 22-harness breadth.

- **Instruction-tier usage ruleset:** `docs/agent-instructions/goldfish-usage.md` generated from `src/instructions.ts` + tool quick reference by the sync script. Users copy it into their repo's instruction surface (AGENTS.md, .cursor/rules/, .windsurf/rules/, .clinerules/, .kiro/steering/) for harnesses that never show MCP server instructions. Revised from the original "AGENTS.md split" — the goldfish repo's own AGENTS.md correctly serves contributors, so its CLAUDE.md mirror stays.
- **Version-tag guard:** `scripts/version-tag-check.ts` (+ `bun run check:version-tag`) asserts an exact release tag on HEAD equals SERVER_VERSION — catches the all-surfaces-stale-together failure the mutual-agreement tests can't (the exact bug ponytail shipped in its v4.8.0). Wired into tests so release-commit verification enforces it automatically.
- **Drift guards:** new `tests/agent-assets.test.ts` — .agents/skills mirror byte-equality with no strays, AGENTS.md contributor mirror, generated usage-doc freshness (regenerates in-memory and diffs), tag agreement.
- **Committed client configs:** working `opencode.json` and `.vscode/mcp.json` (gitignore narrowed with `!.vscode/mcp.json`) — dogfooding in this repo, copyable examples for users. README sections updated to point at them.
- **`docs/agent-portability.md`:** support matrix + deliberate non-support with reasons (no hooks tier — v7.0 removal; no Gemini CLI extension — no evidence; no committed rule-file adapters — they'd only configure this repo; no non-Bun runtime) + uninstall story for `~/.goldfish/registry.json`.
- README gains an "Instruction-Only Harnesses" section with the honest caveat that rules without a registered MCP server have nothing to call.
