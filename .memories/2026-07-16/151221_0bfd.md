---
id: checkpoint_0bfd957f
timestamp: 2026-07-16T15:12:21.290Z
tags:
  - release
  - 7.6.0
  - hooks
git:
  branch: main
  commit: 944a96a
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .claude/worktrees/hooks-tier/.julieignore
    - .codex-plugin/plugin.json
    - CHANGELOG.md
    - README.md
    - package.json
    - src/server.ts
summary: Goldfish 7.6.0 release prepped — hooks tier + Codex one-install +
  conditional recall
type: checkpoint
evidence:
  - "bun test: 617 pass / 0 fail x6 runs"
  - tsc --noEmit clean
  - "bun run check:version-tag: HEAD carries no release tag; nothing to check"
next: Commit release, then tag v7.6.0 + push + gh release on user approval
---

## Goldfish 7.6.0 release prepped — hooks tier + Codex one-install + conditional recall

**WHAT:** Version bumped on all six surfaces (server.ts, package.json, both plugin manifests, marketplace.json, README banner); CHANGELOG gains the 7.6.0 section; README blurb rewritten for the release.

**WHY:** Ships the SessionStart hooks tier (Claude Code + Codex), the Codex one-install plugin (root `.mcp.json` + skills + hooks with documented trust step), and the recall philosophy change (conditional recall; recalled context is historical evidence — verify drift-prone facts).

**HOW:** Minor bump 7.5.0 → 7.6.0 (new feature tier). Verified: 617 tests pass (6 consecutive clean runs; one unreproducible transient failure observed twice today — watch item: capture test names via grep '(fail)' not tail if it recurs), tsc clean, `check:version-tag` vacuously green pre-tag.

**IMPACT:** Release commit next; tag v7.6.0 + push + GitHub release await user approval. Manual smokes still outstanding: Claude Code plugin install session check, Codex install + /hooks trust + tools registration, Windows commandWindows smoke.
