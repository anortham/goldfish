---
id: checkpoint_7ce42a49
timestamp: 2026-08-09T00:59:26.489Z
tags:
  - feature
  - mcp
  - sdk-v2
  - stateless
  - stdio
  - workspace-roots
  - tdd
git:
  branch: mcp-sdk-v2-stateless-migration
  commit: 25c7f7a
  files:
    - src/server.ts
    - tests/protocol-compatibility.test.ts
    - tests/server.test.ts
summary: Enabled dual-era MCP stdio
briefId: mcp-sdk-v2-stateless-migration
type: checkpoint
impact: Modern and legacy stdio behavior are both covered; no protocol context
  crosses into core workspace recovery.
evidence:
  - bun test tests/server.test.ts -t "does not invoke roots when the protocol
    adapter disables it" (pass)
  - bun test tests/protocol-compatibility.test.ts (2 pass)
  - bun test tests/server.test.ts (57 pass)
  - bun run typecheck (pass)
  - bun run build (pass)
symbols:
  - startServer
  - createServer
  - hydrateWorkspaceArguments
  - tests/protocol-compatibility.test.ts
next: Commit the three owned files as `Serve legacy and modern MCP clients over
  stdio`; Task 3 follows for docs/version metadata.
confidence: 5
---

## Enabled dual-era MCP stdio

- **WHAT:** Added the modern stdio compatibility slice in `src/server.ts`, `tests/server.test.ts`, and `tests/protocol-compatibility.test.ts`.
- **WHY:** The v2 SDK's pinned 2026-07-28 client could not negotiate against the direct legacy `StdioServerTransport`; modern requests also needed a provable no-push-roots path.
- **HOW:** Switched `startServer()` to `void serveStdio(() => createServer())`; exported the plain-data `hydrateWorkspaceArguments()` seam; passed `ctx.mcpReq.envelope === undefined` as the legacy roots capability guard; kept explicit/env/cwd recovery and refusal behavior unchanged. Added spawned Bun stdio tests with isolated cwd/HOME/GOLDFISH_HOME and a focused roots callback bypass test.
- **IMPACT:** Legacy roots/cache/recovery tests remain green while real spawned clients negotiate the modern era, list the exact tools, honor workspace precedence, and safely refuse an untrusted cwd. The owned slice is ready to commit; Task 3 can update docs/version metadata afterward.
