---
id: checkpoint_2c6f9e30
timestamp: 2026-08-09T01:38:32.627Z
tags:
  - mcp
  - sdk-v2
  - legacy
  - review-followup
  - test-coverage
git:
  branch: mcp-sdk-v2-stateless-migration
  commit: f53f637
  files:
    - tests/protocol-compatibility.test.ts
summary: Add spawned legacy stdio coverage
briefId: mcp-sdk-v2-stateless-migration
type: checkpoint
evidence:
  - "RED: bun test tests/protocol-compatibility.test.ts failed because
    connectLegacyServer was not defined"
  - "GREEN: bun test tests/protocol-compatibility.test.ts passed 3 tests"
  - Raw legacy initialize/tools-list smoke passed before this coverage addition
symbols:
  - connectLegacyServer
  - MCP legacy stdio compatibility
next: Commit the test and checkpoint together, then rerun the migration
  verification gates.
---

## Add spawned legacy stdio coverage

- **WHAT:** Added `connectLegacyServer()` and a real stdio test that negotiates legacy mode, asserts the legacy protocol era, and lists all three Goldfish tools.
- **WHY:** The external Grok review identified a legitimate coverage gap: existing legacy tests used in-memory transport and did not exercise `serveStdio` opening classification.
- **HOW:** Reused the isolated temporary HOME and GOLDFISH_HOME setup used by modern tests; clients close and temporary directories are removed in `finally`. The reported Bun-parent failure was rechecked locally and did not reproduce.
- **IMPACT:** Legacy compatibility is now covered at the shipped stdio boundary without changing production behavior. Optional roots-cache hardening remains intentionally deferred because malformed responses already fall through the existing catch path.
