---
id: checkpoint_a79f9ffa
timestamp: 2026-09-24T14:40:19.190Z
tags:
  - checkpoint-policy
  - behavioral-language
  - instructions
  - razorback
  - memory-value-audit
git:
  branch: main
  commit: e98511d
  files:
    - .agents/skills/checkpoint/SKILL.md
    - AGENTS.md
    - CHANGELOG.md
    - CLAUDE.md
    - TODO.md
    - docs/IMPLEMENTATION.md
    - docs/agent-instructions/goldfish-usage.md
    - skills/checkpoint/SKILL.md
    - src/instructions.ts
    - src/tools.ts
    - tests/hooks.test.ts
    - tests/server.test.ts
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Selective checkpoint triggers (TODO item 1)
type: decision
context: The transcript audit found commit-time checkpoints were 35% of saves
  but only 16% of those that changed a later decision; 84-90% of used
  checkpoints had a next line. Razorback (b00fba7) already cites "Goldfish's
  selective policy", so Goldfish had to actually state it.
decision: 'Checkpoint triggers are now three: a consequential decision (with the
  rejected option), a surprising failure (with evidence), and unfinished work
  (with next). "When in doubt" and the commit trigger are gone. A warranted
  checkpoint is still written before its commit.'
alternatives:
  - "Keep phase and milestone ends as a trigger: rejected to match Razorback,
    which the user had already changed to 'phase boundaries need no checkpoint';
    its handoff before likely context loss still covers the long-run value"
  - "Keep 'when in doubt' and only drop the commit trigger: rejected because it
    would keep the bias toward extra checkpoints"
impact: "Expect about a third fewer checkpoints. Watch for underuse: CLAUDE.md
  records an earlier underuse regression, so the tone stays positive (\"Don't
  ask permission, do it\") with no Do-NOT lists."
symbols:
  - getInstructions
  - getTools
next: "Get the user's approval, then commit and release 8.0.6: bump the 7
  version surfaces, update the CHANGELOG, commit, tag v8.0.6, push the branch
  and the tag separately, and run check:version-tag."
---

## Selective checkpoint triggers (TODO item 1)

**WHAT:** Rewrote the trigger text in `src/instructions.ts` (now 1,998 of 2,000 characters), the checkpoint tool description in `src/tools.ts`, and `skills/checkpoint/SKILL.md`. Updated the policy text in CLAUDE.md, AGENTS.md, and docs/IMPLEMENTATION.md. Synced the skill mirror and regenerated the usage doc. Tests in `tests/server.test.ts` and `tests/hooks.test.ts` now require the three triggers and the before-commit rule, and reject "When in doubt".

**WHY:** The audit evidence above, and alignment with Razorback's selective policy.

**HOW:** Changed the tests first and saw four fail. The first rewrite of the instructions was 2,206 characters; I shortened it to fit the cap. Razorback needed no change: its skills already use this policy, and the only other "when in doubt" lines are an unrelated rule. The rendered hook payload contains no old trigger.

**IMPACT:** Full suite: 688 pass. Typecheck is clean. Not committed; waiting for approval to commit and release 8.0.6.
