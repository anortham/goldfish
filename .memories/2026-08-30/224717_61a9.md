---
id: checkpoint_61a9e3d8
timestamp: 2026-08-30T22:47:17.148Z
tags:
  - deps
  - typescript
git:
  branch: main
  commit: 55ef5d7
  files:
    - .claude-plugin/plugin.json
    - .mcp.json
    - bun.lock
    - mcp.json
    - package.json
actor:
  harness: cursor-vscode
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Bump @types/bun and TypeScript
briefId: post-7-8-0-evidence-driven-hardening-across-client
type: checkpoint
next: Leave machine-local bun paths in plugin/mcp JSON uncommitted.
---

## Bump @types/bun and TypeScript

### WHAT
Pin `@types/bun` to ^1.4.0 and `typescript` to ^5.9.3. `tsc --noEmit` is clean.

### WHY
User asked to commit and push the leftover working-tree changes.

### HOW
Left `.claude-plugin/plugin.json`, `.mcp.json`, and `mcp.json` unstaged. Those replace portable `bun` / `${PLUGIN_ROOT}` with `/home/murphy/...` paths. Hooks tests require `command: bun` and the plugin-root placeholders.

### IMPACT
Dev type pins move forward. Local MCP path overrides stay on this machine only.
