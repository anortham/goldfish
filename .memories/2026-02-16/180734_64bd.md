---
id: checkpoint_64bd5a2b
timestamp: 2026-02-16T18:07:34.815Z
tags:
  - dogfood
  - config
  - skills
  - hooks
  - mcp
  - setup
git:
  branch: main
  commit: ef9297b
summary: "## Set up goldfish dogfood configuration at user level"
---

## Set up goldfish dogfood configuration at user level

Configured `~/.claude/` to mirror what a plugin user would see, running from source.

- **Skills:** Symlinked all 4 skills (`checkpoint`, `plan-status`, `recall`, `standup`) from source to `~/.claude/skills/` — edits to source are immediately reflected
- **Hooks:** Added `PreCompact`, `SessionStart`, and `PostToolUse` (ExitPlanMode) hooks to `~/.claude/settings.json`
- **MCP Server:** Created `~/.claude/.mcp.json` pointing to `/Users/murphy/Source/goldfish/src/server.ts` for cross-project availability
- **Replaced** outdated Miller-based standup skill with current goldfish version
- **Impact:** New Claude Code sessions in any project will have full goldfish experience (auto-recall on start, auto-checkpoint before compaction, auto-plan-save, all slash commands)
